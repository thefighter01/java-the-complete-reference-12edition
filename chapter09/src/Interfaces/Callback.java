package Interfaces;

public interface Callback {
    void callback(int p); // all methods in the interface are implicitly public
    // all variables in the interface are static final
    // by jdk 8 add variables and static methods
    // by jdk 9 added private methods

}
